name: Karate CI Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Descargar repositorio
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Configurar Java 21
      - name: ☕ Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      # 3️⃣ Levantar el servidor TODO
      - name: 🚀 Start TODO Server
        run: |
          cd karate-todo-main
          echo "📍 Iniciando servidor TODO..."
          nohup mvn clean test -Dtest=LocalRunner > ../server.log 2>&1 &
          SERVER_PID=$!
          echo "Server PID: $SERVER_PID"
          
          # Esperar a que el servidor esté listo
          echo "⏳ Esperando servidor (máximo 60 segundos)..."
          for i in {1..30}; do
            if curl -s http://localhost:8080/health > /dev/null 2>&1 || curl -s http://localhost:8080 > /dev/null 2>&1; then
              echo "✅ Servidor está listo"
              sleep 2
              break
            fi
            echo "  Intento $i/30..."
            sleep 2
          done
          
          # Verificar una última vez
          if ! curl -s http://localhost:8080 > /dev/null 2>&1; then
            echo "❌ ERROR: Servidor no responde"
            echo "📄 Server logs:"
            cat ../server.log
            exit 1
          fi
          echo "✅ Servidor verificado y operativo"

      # 4️⃣ Ejecutar pruebas Karate
      - name: 🧪 Run Karate tests
        run: |
          cd karate-template-main
          echo "🏃 Ejecutando suite de pruebas..."
          mvn clean test -DskipTests=false
        continue-on-error: true

      # 5️⃣ Mostrar logs en caso de error
      - name: 📋 Show server logs on failure
        if: failure()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📄 SERVIDOR LOGS:"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          cat server.log || echo "No server logs found"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      # 6️⃣ Subir reportes HTML
      - name: 📦 Upload Karate HTML Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: karate-html-reports
          path: karate-template-main/target/karate-reports/
          retention-days: 30

      # 7️⃣ Subir reportes XML
      - name: 📊 Upload Surefire Reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: surefire-reports
          path: karate-template-main/target/surefire-reports/
          retention-days: 30

  deploy-report:
    needs: test
    runs-on: ubuntu-latest
    if: always()
    
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: 📥 Download HTML Reports
        uses: actions/download-artifact@v4
        with:
          name: karate-html-reports
          path: report-public/

      - name: 🔧 Prepare index.html for GitHub Pages
        run: |
          cd report-public
          echo "📂 Contenido descargado:"
          ls -la
          
          if [ -f "karate-summary.html" ]; then
            echo "✅ karate-summary.html encontrado"
            cp karate-summary.html index.html
            echo "✅ Copiado como index.html"
          else
            echo "❌ ERROR: karate-summary.html no encontrado"
            echo "📂 Archivos disponibles:"
            ls -la
            exit 1
          fi

      - name: ⚙️  Setup GitHub Pages
        uses: actions/configure-pages@v5

      - name: 📤 Upload Artifacts to Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'report-public/'

      - name: 🚀 Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: 🎉 Deployment Complete
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ REPORTE PUBLICADO"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📍 URL: ${{ steps.deployment.outputs.page_url }}"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
